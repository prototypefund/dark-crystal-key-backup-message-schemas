module.exports = {
  root: [
    require('./root/v1')
  ],
  shard: [
    require('./shard/v1')
  ],
  request: [
    require('./request/v1')
  ],
  reply: [
    require('./reply/v1')
  ],
  forward: [
    require('./forward/v1')
  ]
}
